
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // PERMITIR ACCESO TOTAL DURANTE LA CONFIGURACIÓN
      // Esto asegura que tus subidas no fallen por errores de permisos.
      allow read, write: if true;
    }
  }
}
